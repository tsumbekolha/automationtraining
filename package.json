{
  "devDependencies": {
    "@playwright/test": "^1.16.3"
  },
  "dependencies": {
    "allure-commandline": "^2.13.8",
    "allure-playwright": "^2.0.0-beta.14"
  },
  "scripts": {
    "debug": "PWDEBUG=1 npx playwright test --reporter=list,allure-playwright",
    "test": "npx playwright test --reporter=list,allure-playwright",
    "report": "allure generate ./allure-results --clean && allure open ./allure-report"
  }
}
